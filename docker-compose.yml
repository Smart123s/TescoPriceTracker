version: '3'

services:
  # Currently disabled, we moved to a static web app model
  # webapp:
  #   image: ghcr.io/smart123s/tescopricetracker:latest
  #   container_name: tesco_tracker_web
  #   restart: unless-stopped
  #   command: python app.py
  #   volumes:
  #     - ${DATA_FOLDER}:/app/data
  #   ports:
  #     - "50001:50001"
  #   environment:
  #     - TZ=Europe/Budapest
  scheduler:
    image: ghcr.io/smart123s/tescopricetracker:latest
    container_name: tesco_tracker_scheduler
    restart: unless-stopped
    command: python scheduler.py
    volumes:
      - ${DATA_FOLDER}:/app/data
    environment:
      - API_KEY=${API_KEY}
      - TZ=Europe/Budapest
  watchtower:
    image: containrrr/watchtower
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
    command: --interval 300  # Check every 5 minutes
    restart: always